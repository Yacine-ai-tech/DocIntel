# Lightweight dev compose for DocIntel — meant for a remote dev host (any on-demand
# cloud box, a home server, etc.) — not just a local machine.
# Tesseract/poppler are baked into the image (Dockerfile installs via apt).
#
# Usage on the remote host:
#   docker compose -f docker-compose.dev.yml up --build
#
# Reach it from your laptop: ssh -L 8001:localhost:8001 <your-remote-host>
# Then: http://localhost:8001/health

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: docintel:dev
    volumes:
      - ./core:/app/core
      - ./services:/app/services
      - ./api.py:/app/api.py
      - ./demo:/app/demo
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
    ports:
      - "8001:8001"
    env_file: .env
    environment:
      PYTHONUNBUFFERED: "1"
    command: >
      uvicorn api:app
      --host 0.0.0.0
      --port 8001
      --reload
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
